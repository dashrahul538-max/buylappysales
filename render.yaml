# Render static site configuration
# Deploy this repository as a static site.

services:
  - type: static
    name: buylappysales
    env: static
    branch: main
    buildCommand: ""
    publishPath: "."
    plan: free
