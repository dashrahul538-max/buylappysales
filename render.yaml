services:
  - type: web
    name: buylappysales
    env: node
    region: oregon
    plan: free
    branch: main
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
